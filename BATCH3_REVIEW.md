# مراجعة Batch 3 — الوصفات والتصنيع واستهلاك الخامات

نُفذت Batch 3 وطُبقت migration [0019_recipes_and_manufacturing.sql](supabase/migrations/0019_recipes_and_manufacturing.sql) على Supabase المرتبط. لم يبدأ العمل على Batch 4 أو حسابات العمال، ولم يُستدعَ DeepSeek.

## ما أصبح متاحًا

- `/dashboard/product-specs`: إنشاء وتعديل الوصفات، إضافة عدة خامات بكميات للوحدة الواحدة، تنشيط/تعطيل الوصفة وحذف الوصفات غير المستخدمة. الخامة لا تتكرر في نفس الوصفة. حفظ الرأس والأسطر عملية واحدة، فلا يترك فشل سطر وصفة محفوظة جزئيًا. الوصفة المرتبطة بأوردر يحميها FK من الحذف؛ يمكن تعطيلها للأوردرات الجديدة.
- إنشاء أوردر بوصفة من `/dashboard/orders/new/recipe`، مع عرض إجمالي كل خامة = كمية الوحدة × عدد الوحدات، وإدخال تجاوز لإجمالي الخامة لهذا الأوردر فقط. الرابط موجود في شاشة إنشاء الأوردر القديمة، التي تظل متاحة للأوردرات المخصصة بلا وصفة.
- الأوردر يحتفظ بـ `product_spec` JSONB كما كان، وبـ `product_spec_id` اختياري. تحفظ `material_requirements` نسخة من الخامات وأسمائها ووحداتها وكميات الوصفة الافتراضية عند الإنشاء، وتحفظ `material_overrides` التجاوزات منفصلة. تعديل الوصفة لاحقًا لا يعيد حساب أوردر محفوظ، حتى لو لم يبدأ بعد.
- `/dashboard/orders/[id]`: عرض الاحتياجات والحركات الفعلية، وتعديل التجاوز عند بدء التصنيع، ثم إتمام التصنيع والتسليم. لا خصم عند إنشاء الأوردر. التجاوز هو إجمالي كمية الخامة للأوردر، وليس كمية للوحدة؛ تركه فارغًا يستخدم الافتراضي المحفوظ، وصفر يتخطى استهلاك ذلك السطر. لا يوجد تحويل ضمني بين الكيلو والطن.
- لا يمكن تعديل وصفة الأوردر أو عدد وحداته بعد حفظه؛ يمكن تعديل كميات الخامات الخاصة به قبل البدء. بعد بدء التصنيع تُمنع تغييرات التجاوز والعودة لحالة سابقة لمنع استهلاك نفس الأوردر مرتين.

## الحالة الحالية والانتقالات

كان `orders.status` موجودًا فعلًا بالقيم `pending / in_production / completed / cancelled`. أُضيف `delivered` دون عمود حالة مكرر. التسلسل الآن:

`pending → in_production → completed → delivered`

يمكن إلغاء الأوردر من `pending` فقط، وتبقى قيمة `cancelled` القديمة محفوظة. قبل الترحيل كان هناك أوردر حي `pending` وآخر `in_production`؛ لم تتغير حالتهما ولم تُولّد لهما وصفة أو حركات بأثر رجعي. البيع المباشر القديم الذي ينشئ أوردرًا مخصصًا `completed` يستمر في العمل.

الأوردر بلا وصفة يتابع نفس الحالات، مع **صفر استهلاك تلقائي**؛ يظل تسجيل الخامات يدويًا من المسار السابق. إتمام التصنيع أو التسليم لا يخصم أي خامات إضافية.

## العملية الذرية ومنع التكرار

`orders_manufacturing_guard` يعمل قبل INSERT/UPDATE؛ لذلك تعديل الحالة المباشر من SQL يمر بنفس الضمانات، وليس فقط Server Action.

عند الانتقال من `pending` إلى `in_production` لأوردر بوصفة، يقفل صفوف جميع الخامات بترتيب UUID ثابت، ويفحص المطلوب مقابل المتاح لكل سطر. يعرض النقص باسم الخامة والمطلوب والمتاح والعجز. بعد نجاح الفحص فقط يستدعي `record_material_movement` الموجود لكل كمية موجبة، باستخدام `out / is_return=false / order_id`.

**لم تُضف أي كتابة جديدة إلى `materials.stock_qty`.** الكاتب ما زال INSERT trigger الخاص بحركات الخامات الذي أُصلح في Batch 1. الـ trigger الجديد ينظم إنشاء الحركات فقط. الحركات والخصم وتغيير الحالة كلها في معاملة واحدة؛ أي فشل يلغيها جميعًا. تكرار طلب نفس الحالة لا يكرر الحركات.

أُضيف القيد `UNIQUE(order_id)` باسم `invoices_order_id_key`. الفحص السابق للتطبيق لم يجد أي تكرارات، فلم تُحذف أو تُدمج فواتير قديمة.

## أدلة التحقق الحي

استُخدم Chrome مع التطبيق المبني وSupabase الحي. الوصفة والخامات التالية بيانات اختبار مؤقتة منفصلة، وليست وصفة تشغيل مقترحة للمصنع:

| الخطوة | خامة A بالكيلو | خامة B بالطن | نتيجة الأوردر |
|---|---:|---:|---|
| البداية وإنشاء الأوردر | 100 | 10 | `pending`، دون خصم |
| وصفة 3 كجم + 0.5 طن للوحدة، أوردر 4 وحدات | **100 → 88**، حركة صادر 12 | **10 → 8**، حركة صادر 2 | `in_production`، حركتان فقط |
| إعادة طلب بدء نفس الأوردر | 88 → 88 | 8 → 8 | لا حركة إضافية |
| أوردر ثانٍ بوحدتين، تجاوز A من 6 إلى 9 | **88 → 79**، حركة صادر 9 | **8 → 7**، حركة صادر 1 | الوصفة الأصلية لم تتغير |
| أوردر 20 وحدة يحتاج A=60 وB=10 | **79 → 79** | **7 → 7**، العجز 3 | رُفض البدء، ظل `pending`، **صفر حركات** |
| إتمام الأوردر الأول وتسليمه | 79 → 79 | 7 → 7 | وصل `delivered` دون خصم جديد |

اختبار تزامن حي إضافي: خامة مستقلة رصيدها **10 كجم**، وأوردران يحتاج كل منهما **7 كجم**. أُرسلت محاولتا البدء بالتوازي إلى القاعدة: **نجحت واحدة فقط**، ورُفضت الثانية لنقص المخزون؛ الرصيد النهائي **3 كجم**، وعدد الحركات **1**.

اختبار الفاتورة المكررة تجاوز التطبيق بالكامل: شُغّل **INSERT SQL مباشر** لفاتورتين برقمين مختلفين ونفس `order_id` داخل معاملة اختبار على PostgreSQL الحي. الثانية رُفضت بـ **SQLSTATE 23505**، وأكد `GET STACKED DIAGNOSTICS` أن السبب تحديدًا `invoices_order_id_key`؛ انتهت المعاملة بـ ROLLBACK. تأكد نفس الخطأ أيضًا من إدخال مباشر للجدول عبر Supabase، دون دالة إصدار الفاتورة.

فُحص من المتصفح إنشاء الوصفة وتعديلها وتعطيلها، رفض حذفها بعد استخدامها، وحذف وصفة غير مستخدمة. تغيير الوصفة بعد حفظ الأوردر لم يغير نسخته. واختبار SQL تحقق كذلك من المسار المخصص اليدوي، منع الرجوع للخلف، رفض تعديل الاحتياجات بعد البدء، ورفض استخدام وصفة غير نشطة لأوردر جديد.

الأدلة: [نتائج الاختبار الحي وUUIDs](audit/batch3/live-Batch3-d4777b91-3887-4b6b-b944-76a8fea71c4f.json)، [صورة رسالة نقص الخامة](audit/batch3/shortage-Batch3-d4777b91-3887-4b6b-b944-76a8fea71c4f.png)، [اختبار SQL القابل للإعادة](scripts/verify-batch3.sql).

## التنظيف والمراجعة النهائية

حُذفت حركات وأوردرات وفاتورة وعميل ووصفات وخامات الاختبار والحساب المؤقت. الخامات التجريبية كانت منفصلة عن خامات المصنع؛ بعد التنظيف طابقت أرصدة الخامات الأصلية والفواتير الأصلية ما قبل الاختبار. لم يُستهلك أي رقم `INV`، وبقي عداد 2026 عند **3** دون تغيير؛ فجوة `0003` السابقة باقية.

أُعيدت مقارنة catalogs الحية بإعادة تشغيل `0001` حتى `0019`: **صفر فروق** في الحقول المقارنة. الجرد يشمل 17 جدول تطبيق وview واحدة؛ أداة المقارنة تعرض 18 ضمن `tables` لأنها تشمل الـ view. توجد الآن دالتا trigger: القديمة كاتبة المخزون، والجديدة المنظمة للتصنيع. [المقارنة الحالية](audit/schema/comparison.json) و[لقطة ما قبل Batch 3](audit/batch3/schema-before.json).

فحوص التسليم نجحت:

- `npm.cmd run build`
- `node node_modules/typescript/bin/tsc --noEmit`
- `npm.cmd run lint -- . --max-warnings=0`
- `npm.cmd run verify:batch1`
- `npm.cmd run verify:batch2`
- `npm.cmd run verify:batch3`
- `npm.cmd run verify:batch3:live`
- `node scripts/audit-schema.mjs`

لم يُنشر التطبيق على استضافة خارجية. التوقف هنا لمراجعة Batch 3 قبل أي عمل على Batch 4.
