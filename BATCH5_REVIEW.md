# مراجعة Batch 5 — التصحيح، أخطاء القراءة، والنسخ الاحتياطي

نُفذت أدوات التصحيح وتنظيف أخطاء القراءة، وطُبقت [0021_correction_history.sql](supabase/migrations/0021_correction_history.sql) على Supabase بعد معاينة أظهرت أنها migration الوحيدة المعلقة. **النسخ الاحتياطي ما زال شرط تشغيل غير محقق:** الحساب Free، وقائمة النسخ المتاحة فارغة وPITR غير مفعّل. لم تبدأ دفعة أخرى ولم يُستدعَ DeepSeek.

## أدوات التصحيح

من قائمة العمال، اضغط اسم العامل لفتح `/dashboard/workers/[id]`. الصفحة تتيح تعديل اليومية مع تأكيد، واختيار أسبوع لتصحيح حضوره ومراجعة تاريخ صرفه. يوجد رابط إليها أيضًا من شاشة الرواتب.

- تعديل اليومية يؤثر في الحسابات غير المصروفة؛ سجلات الصرف السابقة تحتفظ باليومية والتفصيل المحفوظين.
- تعديل الحضور يشمل الحالة ونوع الإضافة وقيمتها. إذا وُجد صرف غير ملغى للأسبوع تمنع الواجهة التعديل، ويمنعه trigger داخل القاعدة أيضًا. الحماية تشمل INSERT/UPDATE/DELETE المباشر، ولا تسمح بنقل سجل محفوظ إلى عامل أو يوم آخر. تقفل صف العامل، وهو نفس القفل المستخدم عند حساب الصرف.
- إلغاء الصرف يتطلب سببًا وتأكيدًا. تحتفظ القاعدة بالأصل وتملأ `voided_at` من توقيتها، ولا تسمح بإعادة تفعيل السجل أو تعديل تفاصيله أو حذف السجل. يمنع trigger كذلك TRUNCATE للجدول. استُبدل القيد القديم بفهرس فريد جزئي بنفس الاسم `worker_payouts_worker_week_key`، يشمل فقط `voided_at IS NULL`؛ يمكن إعادة الصرف بعد التصحيح دون حذف التاريخ.
- كانت المرتجعات بلا إلغاء. أصبح إلغاء المرتجع متاحًا من تفاصيل الفاتورة بنفس ضمانات الحفظ والسبب. الحساب وحدّ المرتجعات الجديدة يستبعدان الملغى. يظهر المرتجع الأصلي في الفاتورة وكشف العميل بمبلغه وسبب إلغائه ودائن فعلي صفر. لا كتابة في الخامات أو حركاتها.
- صلاحيات التصحيح تستخدم حسابات لوحة التحكم المصادق عليها وRLS الحالي، دون مفتاح خدمة في التطبيق. لا يوجد نظام أدوار مدير/موظف منفصل في المشروع؛ لم ندّعِ إضافته في هذه الدفعة.

الإلغاء يصحح السجل، ولا يسترد مبلغًا نقديًا تلقائيًا. توضح الشاشة ضرورة مراجعة التسوية الفعلية قبل إعادة الصرف. الصافي السالب ما زال يمنع الصرف طبقًا لاختيار المستخدم.

## الأرقام والتحقق من البداية للنهاية

المثال على أسبوع الجمعة 4 إلى الخميس 10 سبتمبر 2026. التوقعات التالية assertions صريحة في [اختبار SQL](scripts/verify-batch5.sql) قبل التنفيذ:

| الخطوة | الحساب اليدوي | نتيجة PostgreSQL والمتصفح |
|---|---:|---:|
| يوم نصف حضور، يومية 400، إضافة مبلغ 50 | 0.5 × 400 + 50 | 250 |
| تعديل اليومية إلى 500 | 0.5 × 500 + 50 | 300 |
| تصحيح الحضور إلى حاضر وإضافة نصف يوم | 500 + 0.5 × 500 | 750 |
| صرف الأسبوع | snapshot يومية 500 وصافي 750 | 750 محفوظة |
| تعديل اليومية الحالية إلى 600 بعد الصرف | الحساب الحالي 900؛ snapshot السابقة لا تتغير | المحفوظ 750 / يومية 500 |
| محاولة تعديل الحضور في الأسبوع المصروف | رفض | رُفض دون تعديل |
| إلغاء الصرف، ثم تصحيح الحضور لربع يوم وإضافة مبلغ 25 | 0.25 × 600 + 25 | 175 |
| إعادة صرف نفس الأسبوع | أصل ملغى 750 + سجل فعّال 175 | سجلان، كلاهما ظاهر في التاريخ |
| فاتورة 1000 ومرتجع 100 | 1000 − 100 | 900 |
| إلغاء المرتجع | 1000 − 0 | 1000، والمرتجع الأصلي ظاهر بدائن صفر |

اختبار **Chrome على التطبيق المبني** نفذ النماذج وServer Actions الفعلية، وفحص الأرصدة المحفوظة وعرض السجلين والمرتجع الملغى. قاعدة هذا الاختبار PostgreSQL معزولة عبر PGlite مع كل migrations؛ طبقة Auth/REST محوّلة إلى بوابة اختبار محلية، والشبكة إلى المصنع محظورة. **هذا ليس اختبار تكامل Auth/PostgREST حيًا**، ولا نعرضه بهذه الصفة. [دليل المتصفح](audit/batch5/browser.json)، [صورة التاريخ](audit/batch5/correction-history.png).

تكرر الاختبار مباشرة على **PostgreSQL الحي** باستخدام دوري authenticated وservice_role داخل معاملة انتهت بـ ROLLBACK. أثبت رفض INSERT مكرر بـ `23505` ومن الفهرس `worker_payouts_worker_week_key` تحديدًا، ورفض الحضور بعد الصرف، ومنع إعادة تفعيل الملغى والحذف، حتى مع service_role. تحقق أيضًا أن المرتجع الملغى لا يستهلك حد المرتجعات. [مخرجات تشغيل SQL الحي](audit/batch5/sql-live-raw.json)؛ نجاح الأمر بلا صفوف ناتج عن assertions داخل DO وROLLBACK، وليس رسالة «نجاح» مزروعة في السكربت.

محاولات المتصفح الأولى كشفت مشكلتين في محددات الاختبار: نموذج الإلغاء يختفي بعد النجاح، وNext يضيف عنصر alert آخر للإعلان عن التنقل. صُححت assertions للتحقق من حالة التاريخ وعنصر الخطأ داخل main، ثم نجحت الدورة الكاملة. لا نعد المحاولات السابقة نجاحًا. لم تُترك سجلات تجريبية حية ولم يُحذف أي سجل صرف أو مرتجع؛ محاولات DELETE المقصودة رُفضت.

## تنظيف أخطاء SELECT كاملًا

جُردت كل قراءات Server Components داخل `app/(dashboard)`، بما فيها الاستعلامات المحفوظة في متغيرات ونتائج Promise.all وRPC القراءة: **36 موضع قراءة في 23 ملف TSX**. تتبعت معالجة الخطأ لكل نتيجة قبل عرضها؛ لا توجد قراءة SELECT متبقية في هذا النطاق تعرض البيانات دون التعامل مع error. الجرد والنصوص الداعمة: [dashboard-read-inventory.json](audit/batch5/dashboard-read-inventory.json). هذا تدقيق لمسارات الخطأ، وليس ادعاءً بأن كل شاشة تملك ترقيم صفحات غير محدود.

هذه **كل الملفات السبعة التي أُصلحت فيها أخطاء القراءة المتبقية**، نسبة إلى `app/(dashboard)/dashboard/`:

| الملف | الفشل الذي أصبح ظاهرًا | حقن الفشل |
|---|---|---|
| `page.tsx` | ملخص الخامات وعروض الأسعار؛ لا تظهر عبارة المخزون ممتاز | الخامات والعروض، كل منهما مستقل |
| `materials/page.tsx` | قائمة الخامات | الخامات |
| `materials/movements/new/page.tsx` | قوائم الخامات والموردين؛ لا يُعرض نموذج إدخال ناقص | كل قائمة مستقلًا |
| `suppliers/page.tsx` | قائمة الموردين وإجمالي المستحقات | الموردون |
| `quotations/page.tsx` | قائمة العروض | العروض |
| `invoices/create/page.tsx` | العملاء والفواتير السابقة والطلبات | كل استعلام مستقلًا |
| `clients/[id]/edit/page.tsx` | فصل خطأ القراءة عن عدم وجود العميل؛ استخدام maybeSingle | قراءة العميل |

**11 حقن فشل مستقلًا على الشاشات السبع** نجحت في اختبار مكونات الصفحات، وتكررت في المتصفح. أضيفت 3 حالات لقراءات صفحة تصحيح العامل الجديدة: العامل والحضور والصرف؛ إجمالي المتصفح **14**. [دليل الحقن](audit/batch5/select-failures.json) ونتائج المتصفح المذكورة أعلاه. لا نعتبر القائمة الفارغة الصحيحة خطأ؛ الاختبارات تعيد error صريحًا وتتحقق من ظهوره.

ملفات التطبيق الأخرى المعدلة/المضافة لهذه الدفعة: `workers/page.tsx` و`workers/payouts/page.tsx` لروابط التصحيح، `workers/[id]/page.tsx` للتصحيح والتاريخ، `invoices/[id]/page.tsx` لإلغاء المرتجع، و`corrections/actions.ts` و`corrections/form.tsx` للنماذج والحفظ. أُحدّث `types/database.ts` و`package.json` وأضيفت migration وأدوات/أدلة التحقق.

## النسخ والاستعادة

أكد المستخدم من صفحة Billing أن الباقة **Free**. قراءة الإعدادات الفعلية أعادت `backups=[]` و`pitr_enabled=false` و`physical_backup_data={}`؛ `walg_enabled=true` لا يعني توفر نسخة للاستعادة للمستخدم. [الإعدادات المحفوظة](audit/batch5/backup-settings.json).

**لم ننفذ استعادة: لا توجد نسخة متاحة، والباقة الحالية لا توفر مسار النسخ المُدار المطلوب.** لا يوجد زمن استعادة مقاس، ولا تصدير دوري مستقل مثبت. وثقنا الإجراءات وشروط قبول الاسترجاع ونافذة فقد البيانات في [BACKUP_AND_RECOVERY.md](BACKUP_AND_RECOVERY.md)، مع المصادر الرسمية. القرار المتبقي لصاحب المصنع: ترقية ثم اختبار استعادة إلى مشروع منفصل، أو تجهيز تصدير مستقل دوري واختبار استرجاعه. لم يتغير الاشتراك ولم تحدث استعادة إلى المشروع الحي.

## سلامة البيانات ومطابقة schema والفحوص

بصمات العمال والحضور والمعاملات والصرف والمرتجعات والفواتير وأرصدة الفواتير والخامات وحركاتها وعداد الفواتير **متطابقة قبل وبعد**. الموجودون 3 عمال وسجل حضور واحد؛ الصرف والمرتجعات الأصلية كلاهما صفر صفوف. لم يُستهلك رقم INV ولم تُمس فجوة الترقيم القديمة. [الدليل](audit/batch5/data-verification.json).

إعادة migrations من `0001` إلى `0021` ومقارنتها بالـ catalogs الحية أعطت **صفر فروق** في الحقول المقارنة: 18 جدول تطبيق وview واحدة، 140 عمودًا، 68 قيدًا، 51 فهرسًا، 22 سياسة، 8 triggers، و16 دالة. [المقارنة](audit/schema/comparison.json) و[لقطة ما قبل الدفعة](audit/batch5/schema-before.json).

نجحت فحوص التسليم: `npm.cmd run build`، و`tsc --noEmit`، و`eslint . --max-warnings=0`، والاختبارات المعزولة للدفعات 1 إلى 5، واختبار متصفح Batch 5، وSQL الحي داخل ROLLBACK، ومراجعة schema. اختبارات AI في Batch 1 معزولة فقط؛ DeepSeek الحي يظل مؤجلًا بطلب المستخدم.

سكربتا الاختبار الحي القديمان للدفعتين 2 و4 يرفضان التشغيل قبل إنشاء fixtures عند وجود migration الجديدة؛ تنظيفهما القديم يعتمد على DELETE لسجلات مالية، وهو لم يعد مقبولًا. الاختبار المالي القديم استبدل حذف المرتجع بإلغائه. حدّثت بيئة PGlite لتعكس BYPASSRLS وصلاحيات service_role اللازمة لاختبار منع الحذف داخل trigger نفسه. الأدوات الجديدة لا تحذف تاريخًا حيًا.

إعادة التحقق:

```powershell
npm.cmd run verify:batch5
npm.cmd run verify:batch5:browser
node scripts/verify-all-batches.mjs
npm.cmd run build
node node_modules/typescript/bin/tsc --noEmit
npm.cmd run lint -- . --max-warnings=0
```

## التقييم الصريح قبل أسبوع التجربة

**وظائف دورة العميل → الأوردر → التصنيع → التحصيل → الحضور → الصرف وتصحيحه قابلة الآن لأسبوع تجربة مراقَب بالتوازي مع المرجع الحالي.** لا توجد مشكلة إضافية معروفة في المسارات التي اختبرناها تمنع هذه التجربة المشروطة.

**لا أوصي بالنظام نسخةً وحيدة للبيانات بعد.** يجب حسم النسخ الاحتياطي وإثبات الاسترجاع، ومطابقة رصيد الخامات الافتتاحي بالجرد الفعلي؛ إصلاحات الخصم القديمة لم تصلح أرصدة تاريخية تلقائيًا. لم تُنشر نسخة خارجية أو تُختبر استمرارية تشغيلها، فالتجربة الحالية تحتاج جهاز تشغيل وإجراء واضح للتسجيل الموازي.

كذلك لا نعمم هذا الاعتماد على دورة الموردين: ملاحظة Batch 1 عن `record_supplier_transaction` وقبوله نوعًا غير معروف دون تحديث الرصيد لم تُصلح في هذه الدفعة. إذا كان أسبوع التجربة سيعتمد أرصدة الموردين مرجعًا ماليًا، يجب إغلاقها أولًا؛ لا نخفيها خلف نجاح تنظيف شاشة الموردين. الحسابات المصادق عليها تتشارك الصلاحيات الحالية؛ تشغيل مستخدمين بصلاحيات متفاوتة يحتاج تصميم أدوار منفصل.

التوقف هنا لمراجعة Batch 5؛ لا دفعة تالية ولا توسيع نطاق دون مراجعة المستخدم.
