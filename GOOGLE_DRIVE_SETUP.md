# ربط Google Drive وتشغيل النسخ

الحالة الحالية: **متصل ومختبر حيًا يوم 9 سبتمبر 2026**؛ 18 ملفًا ونسخة مشفرة على Google Drive. المزامنة كل دقيقة أثناء تشغيل الجهاز وتسجيل الدخول واتصال الإنترنت. خطوات الربط التالية مرجع لإعادة التجهيز فقط؛ ليست مطلوبة مجددًا. راجع READINESS_REVIEW.md لروابط الملفات والدليل.

## خطوة الربط من حساب صاحب المصنع

1. شغّل `npm.cmd run sync:setup` إن لم توجد النسخة المحلية المجهزة. الملف `.private/google-drive-setup.gs` يحتوي سر ربط؛ لا ترفعه إلى Git ولا ترسله في المحادثة.
2. افتح https://script.google.com وأنشئ مشروعًا باسم «نسخ المصنع». استبدل Code.gs بمحتوى الملف المحلي المذكور.
3. اختر Deploy → New deployment → Web app. اختر Execute as: **Me**، وWho has access: **Anyone**. الوصول العام مطلوب لتلقي الطلبات من المهمة؛ توقيع HMAC بمفتاح سري هو الذي يسمح بتنفيذها، وليس معرفة الرابط وحدها.
4. وافق على صلاحيات Drive/Sheets للمشروع الذي أنشأته بنفسك. انسخ Web app URL المنتهي بـ `/exec`.
5. أضف سطرًا إلى `.env.local`: `GOOGLE_SYNC_URL=https://script.google.com/macros/s/…/exec`. لا تغير الملف السري ولا تضع رابط `/dev`.
6. شغّل `npm.cmd run sync:run -- --full`. لا تعتبر الربط ناجحًا إلا بعد رؤية الملفات فعليًا في مجلد **نسخ نظام المصنع** والتحقق من حالة الاتصال في `/dashboard/integrations`.

النظام ينشئ 18 ملف Google Sheets، واحدًا لكل جدول أعمال: العملاء والأوردرات والفواتير والمدفوعات والشيكات والمرتجعات والخامات وحركاتها والوصفات وأسطرها والموردين ومعاملاتهم والعمال والحضور ومعاملاتهم والصرف والعروض وعداد الفواتير. الفواتير والموردون يشملان الرصيد المحسوب. لا تُصدّر كلمات المرور أو جداول Auth أو رموز مشاركة العروض إلى Sheets.

كل ملف يعرض الحالة الحالية. الإلغاء محفوظ كحالة داخل السطر. إزالة سجل مسموح بحذفه من النظام تزيله من مرآة الجدول؛ هذه الملفات ليست سجلًا تاريخيًا بديلًا. التعديل في Google لا يرجع للنظام، وتستبدله المزامنة التالية. تستطيع تنزيل أي ملف بصيغة Excel من Google.

## التشغيل التلقائي

`scripts/install-background-jobs.ps1` يسجل مهمتين باسم `FactoryERP-backup` و`FactoryERP-sync` لحساب Windows الحالي، بصلاحيات عادية ونافذة مخفية:

- نسخة محلية مشفرة يوميًا الساعة 02:00 بتوقيت الجهاز؛ تبدأ عند أول فرصة لاحقة إذا فات الوقت.
- فحص تغييرات Google كل دقيقة؛ لا يبدأ نسخة ثانية إذا كانت محاولة سابقة تعمل. غياب رابط Google يعيد نتيجة 2 صريحة دون إرسال بيانات.
- المزامنة ترسل أيضًا نسخة قاعدة مشفرة إلى Drive كل 24 ساعة بعد نجاح الربط. فترة فقد البيانات هي الزمن منذ **آخر رفع ناجح**، وقد تزيد عن يوم إذا الجهاز مغلق أو الخدمة فشلت.

الجهاز يجب أن يكون شغالًا وحساب Windows مسجلًا والإنترنت متصلًا. هذا ليس تشغيلًا سحابيًا طوال اليوم. بعد نشر التطبيق على خادم دائم يجب نقل المهمة إليه ومراجعة مكان مفتاح التشفير؛ إعدادات جهاز التطوير لا تنتقل مع نشر Next.

الأخطاء تظهر في `.private/backup-task.log` و`.private/sync-task.log` وفي حالة المزامنة باللوحة. التعطل لا يمس عمليات المصنع؛ التغييرات تنتظر في `sync_events` ويعاد إرسالها. لا تمسح السجل يدويًا لإخفاء فشل المزامنة.

## مفتاح الاستعادة

احفظ نسخة مستقلة وآمنة من `.private/backup-key.bin`، خارج هذا الجهاز، مثل وسيط منفصل لدى المدير. فقد المفتاح يجعل ملفات `.dump.enc` غير قابلة للاستعادة. لا تحفظ المفتاح في مستودع الكود أو داخل ملف Sheets عام. نسخة Google وحدها بدون المفتاح ليست خطة استرجاع كاملة.

دليل النسخ والاسترجاع: `BACKUP_AND_RECOVERY.md`.

التوثيق الرسمي: [Apps Script Web apps](https://developers.google.com/apps-script/guides/web)، [Drive service](https://developers.google.com/apps-script/reference/drive)، [HMAC Utilities](https://developers.google.com/apps-script/reference/utilities).
