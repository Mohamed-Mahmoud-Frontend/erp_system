# النسخ الاحتياطي والاستعادة

## الوضع الحالي — 9 سبتمبر 2026

باقة Supabase ما زالت **Free**. نتيجة فحص Batch 5 كانت `backups=[]` و`pitr_enabled=false`؛ لم تحدث ترقية ولم نفترض وجود استعادة مُدارة. [الإعدادات المقروءة](audit/batch5/backup-settings.json). توصي Supabase مستخدمي Free بالتصدير المستقل، وملفات Storage نفسها ليست ضمن نسخة قاعدة البيانات. [التوثيق الرسمي](https://supabase.com/docs/guides/platform/backups).

**تم الآن تنفيذ البديل المستقل فعليًا:** تصدير PostgreSQL كامل، تشفير AES-256-GCM، وجدولة Windows يومية. شُغلت المهمة المسجلة وخرجت بكود 0، وأُعيدت بيانات نسختها إلى PostgreSQL محلي منفصل. تطابقت أعداد وبصمات كل الصفوف في **46 جدولًا** داخل `public` و`auth` و`supabase_migrations`. هذا استرجاع للبيانات الحقيقية، وليس إعادة migrations فقط. الدليل الحالي مع توقيت البداية والنهاية: [restore.json](audit/readiness/restore.json).

نجح رفع نسخة مشفرة إلى Google Drive يوم 9 سبتمبر 2026. أكد Apps Script بصمة الملف بعد قراءته من Drive، وأُثبت استرجاع الأرشيف المحلي المطابق له إلى قاعدة منفصلة (46 جدولًا). لم ننفذ تنزيلًا مستقلًا من Drive؛ المطابقة ببصمة SHA-256. راجع [GOOGLE_DRIVE_SETUP.md](GOOGLE_DRIVE_SETUP.md) وحالة `/dashboard/integrations`. لا تزال هناك ضرورة لحفظ مفتاح الاستعادة خارج الجهاز.

## ما الذي يُحفظ

- `pg_dump --format=custom` يسجل مخطط قاعدة المصدر وبياناته. نستخدم snapshot صريحة من معاملة `REPEATABLE READ READ ONLY`، ويُحسب جرد الصفوف من **نفس snapshot**، فلا تتسبب كتابة متزامنة في مقارنة نسختين مختلفتين.
- تحفظ النسخة في `.backups/erp-<timestamp>-<uuid>.dump.enc`، ويضم الأرشيف بصمات الجداول داخله بصيغة ERPBACKUP2، فتنتقل معه إلى Google. توجد بجانبه أيضًا نسخة manifest بصيغة JSON فيها SHA-256. لا تُرفع الملفات إلى Git. مفتاح التشفير في `.private/backup-key.bin`.
- حسابات Auth، بما فيها hashes كلمات المرور، تدخل الأرشيف المشفر. كلمات مرور أدوار PostgreSQL وإعدادات مشروع Supabase الخارجية وملفات Storage الثنائية لا تدخل النسخة.
- ملفات Google Sheets تعكس 18 جدول أعمال وحالاتها، دون كلمات مرور أو رموز مشاركة عروض الأسعار. هذه مرآة للمتابعة وليست بديلًا للأرشيف المشفر.

## تشغيل نسخة فورية

من مجلد المشروع، مع جلسة Supabase CLI مرتبطة وصالحة:

```powershell
npm.cmd run backup:export
```

لا يحتاج Docker هنا؛ الأدوات المحمولة موجودة في `.tools/postgresql17/pgsql/bin`. النسخة المستخدمة من PostgreSQL tools هي 17.11، والمصدر 17.6. على جهاز جديد أعد تثبيت الأدوات من [صفحة PostgreSQL الرسمية لويندوز](https://www.postgresql.org/download/windows/). لا تنقل كلمات المرور إلى سطر أوامر أو سجل عام.

المهمة `FactoryERP-backup` تعمل يوميًا 02:00 بتوقيت الجهاز. `StartWhenAvailable` يسمح بتشغيل الموعد الفائت عند توافر الجهاز. الحساب الحالي في Windows يجب أن يكون مسجلًا، والإنترنت متاحًا. يمكن تشغيلها من Task Scheduler أو:

```powershell
Start-ScheduledTask -TaskName FactoryERP-backup
Get-ScheduledTaskInfo -TaskName FactoryERP-backup
```

النتيجة 0 تعني نجاح السكربت، ويجب وجود ملف جديد وmanifest مطابق في `.backups`. التفاصيل في `.private/backup-task.log`. النسخة لا تُعد قابلة للاسترجاع لمجرد أن المهمة مسجلة؛ راجع نتيجة التشغيل واختبار الاسترجاع.

## اختبار استرجاع آمن — لا يكتب إلى المصنع

```powershell
npm.cmd run backup:verify
# أو اختيار اسم أرشيف محدد موجود داخل .backups:
node scripts/backup/verify-restore.mjs erp-...dump.enc
```

ينشئ الاختبار cluster جديدًا داخل `.private/restore-<uuid>`، على `127.0.0.1:55432` فقط، بكلمة مرور مؤقتة عشوائية. يفك تشفير الأرشيف، ويسترجع `public` و`auth` و`supabase_migrations` بواسطة `pg_restore --exit-on-error --no-owner`، ثم يقارن أعداد وMD5 لتمثيل جميع الصفوف بالجرد المحفوظ من لقطة المصدر. SHA-256 يوثق ملف النسخة، وGCM يرفض المفتاح أو المحتوى المتلاعب به.

تُنشأ الجداول في قاعدة فارغة وتُحمّل البيانات قبل triggers الموجودة في مرحلة post-data من الأرشيف؛ فلا يُخصم المخزون مرة ثانية ولا يعاد حساب snapshots الصرف. يُختبر التصنيع والتحصيل بصلاحيات الموظف على الوجهة المسترجعة داخل ROLLBACK. أي خطأ يخرج بكود غير صفر. تُحذف الملفات المؤقتة غير المشفرة وكلمة مرور cluster بعد المحاولة، ويوقف الخادم المحلي؛ يبقى مجلد الاختبار محميًا للمراجعة.

**حدود الدليل:** هذه استعادة فعلية لجداول التطبيق وAuth وسجل migrations، على PostgreSQL مستقل. لا تدّعي أنها أعادت تشغيل خدمات GoTrue أو Realtime أو Storage أو إعدادات OAuth والاستضافة محليًا. العناصر التي تديرها منصة Supabase تحتاج إجراءاتها عند النقل إلى مشروع Supabase جديد. تجربة الدخول الحية للمدير والموظف منفصلة في [auth-live.json](audit/readiness/auth-live.json).

## عند فقد الجهاز أو حدوث تلف

1. أوقف إدخال عمليات جديدة وحدد آخر نسخة سليمة تسبق المشكلة. احتفظ بالنسخة التالفة للتحليل ولا تستبدل الأصل.
2. جهّز جهازًا/بيئة منفصلة، ونسخة الكود والأدوات. انسخ الأرشيف إلى `.backups`، وmanifest إن توفر، ومفتاحه إلى `.private/backup-key.bin` في **البيئة الجديدة**؛ لا تستبدل مفتاحًا قائمًا دون التأكد من النسخ التابعة له.
3. شغّل اختبار الاسترجاع المحدد أعلاه، وراجع العدد والبصمات وقواعد الأعمال. النسخ الجديدة ERPBACKUP2 تحتوي الجرد المشفر داخلها ولا تحتاج ملف manifest منفصلًا لقبول المقارنة. النسخ القديمة ERPBACKUP1 تحتاج manifest المصاحب لإثبات تطابق الصفوف.
4. للنقل إلى Supabase جديد، أنشئ **مشروع وجهة منفصلًا**، وراجع أدواره وإصداره وامتداداته وإعدادات Auth/Storage طبقًا [لدليل Supabase للاسترجاع](https://supabase.com/docs/guides/platform/migrating-within-supabase/backup-restore). لا تشغّل استرجاع schema كاملًا عشوائيًا فوق جداول منصة موجودة.
5. بعد فحص الوجهة، حدّث إعدادات التطبيق إليها، واختبر الدخول وكشف الحساب والتصنيع وتاريخ الصرف. وجّه التشغيل للوجهة وافتح الكتابة فقط بعد قبول المقارنة. لا ترجع عداد الفواتير ولا تحاول ملء فجواته.

هذا الإجراء لا يُنفذ استعادة فوق المصدر الحي. استعادة مشروع Supabase مُدار إلى مشروع جديد تظل مسارًا مختلفًا يتطلب الباقة والنسخ المناسبة. [Restore to a New Project](https://supabase.com/docs/guides/platform/clone-project).

## الوقت ونافذة فقد البيانات

مدة اختبارنا محفوظة في `started_at` و`finished_at` بالدليل. هي مدة استرجاع محلي لهذه البيانات الصغيرة، وليست وعدًا بزمن إعادة تشغيل مصنع كامل أو إنشاء مشروع سحابي جديد. نقل إعدادات الخدمات وفحصها يضيف وقتًا مستقلًا.

عند انتظام النسخ اليومية، قد تفقد ما بعد آخر snapshot، حتى قرابة يوم. عند إغلاق الجهاز أو فشل الشبكة/المهمة، قد تزيد الفترة؛ المعيار هو **آخر نسخة ناجحة متاحة خارج الجهاز ومعها مفتاحها**. تحديث Sheets كل دقيقة لا يوفر PITR ولا يستبدل ذلك.

احفظ نسخة آمنة من المفتاح وmanifest خارج جهاز التشغيل. فقد مفتاح التشفير يجعل الأرشيف غير قابل للاسترجاع. لم نفترض أن هذه الخطوة البشرية حدثت بالفعل.
