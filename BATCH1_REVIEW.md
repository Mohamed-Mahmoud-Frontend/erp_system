# مراجعة الدفعة الأولى

الحالة: الإصلاحات منفذة، والبناء وفحوصات الكود نجحت. اختبار نجاح DeepSeek الحي **مؤجل بطلب المستخدم حتى يشحن الحساب**؛ الحساب الحالي أعاد `402 Insufficient Balance`. لم نعتبر الاستجابة التجريبية أو فشل الخدمة نجاحًا.

## التغييرات

- المخزون: الـ INSERT trigger هو المسؤول الوحيد عن الرصيد، ليغطي أيضًا الإدخال المباشر والاستيراد. الـ RPC يحفظ الحركة فقط داخل نفس المعاملة. الصادر العادي يخصم مرة واحدة سواء ارتبط بأوردر أم لا، والمرتجع في الاتجاهين لا يغيّر المخزون القابل للاستخدام. الرصيد غير الكافي يلغي الحركة والتعديل معًا.
- العميل الموجود: الحقول غير المرسلة تُمرر كـ `undefined` بدل نص فارغ؛ يظل نوع العميل مطلوبًا عند إنشاء عميل جديد. أُصلح نوع UUID القابل للقيمة الفارغة في RPC البيع المباشر. كميات الخزانات أعداد صحيحة، والمدفوع لا يتجاوز الإجمالي.
- عروض الأسعار: تحقق `auth.getUser()` مثل لوحة التحكم، والكتابة بعميل المستخدم الخاضع لـ RLS دون مفتاح الخدمة. حد 10 طلبات/دقيقة/مستخدم داخل نسخة الخادم، بما فيها الطلبات الفاشلة. فشل الخدمة أو JSON أو الحفظ يعيد خطأ، دون بيانات بديلة.
- حفظ `parsed_items` ككائن يضم المنتجات وتكلفة النقل؛ إجمالي الشاشة والـ API يستخدم نفس حساب الكسور العشرية. التحقق من مخرجات AI يسبق الحفظ.
- المشاركة: `/quote/<id>?token=<share_token>`، برمز UUID عشوائي مستقل عن رقم العرض. عميل anonymous منفصل يمرر الرمز إلى سياسة SELECT التي تكشف صفًا واحدًا فقط. غياب الرمز أو خطؤه لا يكشف العرض. لا توجد صلاحية تعديل/حذف للزائر، ولا يستطيع اختيار رمز المشاركة عند إدخال طلب عام. الصفحة ممنوعة من الفهرسة وترسل سياسة referrer تمنع تسريب الرمز عبر الإحالة.
- البناء: Cairo أصبح أصلًا محليًا عبر `@fontsource-variable/cairo`، فلا يوجد تنزيل من Google وقت البناء. أُصلحت أخطاء الأنواع وZod وروابط التنقل، وأزيلت casts وتعطيلات ESLint الموجودة في استدعاءات RPC. تصحيح `searchParams` في صفحة الرواتب يخص توافق Next.js فقط؛ حسابات الرواتب لم تتغير.
- أزيلت إشارة لصورة `/poly.png` غير الموجودة التي كانت تختفي عند فشل التحميل، بدل ترك طلب صورة مكسور أو إسكات تحذير ESLint.

## قاعدة البيانات

تم تطبيق `0014_single_stock_adjustment.sql` و`0015_quotation_items_and_sharing.sql` على مشروع Supabase المرتبط بعد معاينة التغييرات. تبين أن `parsed_items` موجود بالفعل في القاعدة الحية كـ JSONB لكنه غير مسجل في migrations القديمة؛ لذلك migration الجديدة تدعم وجوده وتحافظ على البيانات وتحوّل المصفوفات القديمة إلى الشكل الجديد.

لا تصحح هذه الدفعة تلقائيًا أرصدة تاريخية سبق أن تأثرت بالخصم/الإضافة المزدوجة. كذلك تكلفة نقل أسقطها الكود القديم لا يمكن استعادتها من المصفوفة المحفوظة وحدها.

## أدلة التحقق

| الفحص | النتيجة |
|---|---|
| `npm run build` | نجح بالكامل، 26 صفحة، دون فشل خطوط |
| `tsc --noEmit` | نجح |
| `eslint . --max-warnings=0` | نجح بلا أخطاء أو تحذيرات |
| SQL المخزون المعزول | وارد 100→120، صادر 120→107.5، صادر غير مرتبط→100، مرتجع وارد وصادر يبقيان 100، إدخال مباشر صادر→90، رفض 91 دون حفظ حركة |
| SQL المخزون على Supabase الحي | نفس الاختبار نجح داخل معاملة انتهت بـ ROLLBACK |
| FormData → Server Actions → PostgreSQL معزول | أوردر بعميل موجود وآخر جديد؛ بيع مباشر بعميل موجود وآخر جديد؛ الأرصدة 3800 و2000 صحيحة |
| متصفح Chrome + Supabase الحي | تسجيل الدخول بحساب مؤقت، أوردر كمية 2 لعميل موجود، بيع مباشر 5000 ومدفوع 1200 ومتبقي 3800 |
| HTTP على التطبيق المبني دون جلسة | 401، ولا يوجد استدعاء AI |
| AI معزول باستجابة خدمة مضبوطة | نجاح 200 وحفظ النقل 4500 والإجمالي 9000؛ فشل متعمد 502 دون إدخال؛ JSON غير صالح 502؛ منتجات ناقصة 422؛ حد الطلبات 429 |
| AI حي بمفتاح متعمد الخطأ | 502 دون حفظ عرض |
| AI حي بالمفتاح الحالي | **لم ينجح: DeepSeek أعاد 402 Insufficient Balance** |
| مشاركة عبر متصفح anonymous وقاعدة حية | الرمز الصحيح يعرض الإجمالي 9000؛ رقم العرض وحده أو رمز خاطئ يعيدان 404 |
| RLS في PostgreSQL معزول | حتى SELECT غير مفلتر يكشف صفًا واحدًا فقط مع الرمز؛ لا قراءة لصف آخر ولا تعديل/حذف/اختيار رمز؛ الإدخال العام ما زال يعمل |

اختبار المشاركة الحي المستقل استخدم عرضًا مسمى بوضوح كبيانات اختبار؛ لم يكن بديلًا لاختبار نجاح AI. حُذفت سجلات الاختبار وحساب المستخدم المؤقت بعد التشغيل. فاتورة البيع المباشر التجريبية استهلكت رقمًا واحدًا، ولم نرجع العداد للخلف. لم يتم نشر نسخة التطبيق على استضافة خارجية.

## فحص ابتلاع الأخطاء والنجاح المضلل

تم البحث في `app` و`lib` و`scripts` وملفات SQL. الحالات التالية خارج إصلاحات هذه الدفعة ما لم يذكر خلاف ذلك:

1. **مسار AI القديم — أُصلح:** كان catch يحوّل فشل الخدمة إلى بيانات تجريبية ثم نجاح. أزيل بالكامل.
2. **`lib/supabase/server.ts`، `setAll`:** catch عام يبتلع أخطاء كتابة الكوكيز. القصد دعم Server Components التي لا تسمح بكتابة الكوكيز؛ لكنه لا يميز الخطأ المتوقع من غيره. لا يعيد رسالة نجاح عملية تجارية.
3. **`supabase/migrations/0007_invoice_atomic_numbering.sql`:** `EXCEPTION WHEN OTHERS` أثناء ترحيل أرقام الفواتير القديمة يتجاهل كل الأخطاء داخل الكتلة، وليس أخطاء تحليل الرقم فقط؛ قد يخفي فشل تهيئة التسلسل. لم يُغير.
4. **`app/(public)/quote/actions.ts`:** الـ honeypot يعيد نجاحًا دون حفظ إذا امتلأ الحقل المخفي. هذا مقصود لمكافحة الروبوتات، وليس فشل قاعدة بيانات تم إخفاؤه. بقي كما هو.
5. **`app/(dashboard)/actions.ts`:** تسجيل الخروج لا يفحص `error` المرجع من Supabase قبل التحويل إلى صفحة الدخول؛ قد يبدو الخروج ناجحًا رغم فشل إبطال الجلسة.
6. **`scripts/seed-sample-data.ts`:** استعلاما رصيد المخزون قبل/بعد الحركة لا يفحصان أخطاء القراءة، ورسالة `Trigger verified` تطبع النجاح دون assertion على النتيجة. لم نعتمد عليه في التحقق؛ أضفنا `scripts/verify-stock.sql` باختبارات فعلية تفشل عند أي اختلاف.
7. **`app/(dashboard)/dashboard/clients/actions.ts`، تعديل العميل:** يفحص خطأ UPDATE لكن لا يتحقق من عدد الصفوف المتأثرة؛ مع UUID غير موجود قد يعيد التوجيه كأن التعديل حدث.
8. **`supabase/migrations/0010_material_rpc.sql`، `record_supplier_transaction` واستدعاؤه في `suppliers/actions.ts`:** لا توجد قائمة أنواع محكمة على مستوى القاعدة، والنوع غير المعروف قد يُحفظ دون تحديث الرصيد بينما يعود الاستدعاء بنجاح. إصلاح دورة الموردين خارج هذه الدفعة.
9. **شاشات تتجاهل أخطاء SELECT:** قد تعرض قوائم فارغة أو أصفارًا بدل خطأ. الأماكن التي وجدناها:
   - `app/(dashboard)/dashboard/page.tsx`: الخامات والعروض؛ فشل الخامات قد يظهر أن المخزون ممتاز.
   - `dashboard/materials/page.tsx` و`dashboard/materials/movements/new/page.tsx`: المواد وقوائم الاختيار.
   - `dashboard/suppliers/page.tsx`: قد يظهر إجمالي المستحقات صفرًا.
   - `dashboard/workers/page.tsx` و`dashboard/workers/attendance/page.tsx` و`dashboard/workers/payouts/page.tsx`: العمال والحضور والمعاملات؛ فشل القراءة قد ينتج صافيًا مضللًا.
   - `dashboard/orders/page.tsx` و`dashboard/orders/new/page.tsx`: الطلبات وقائمة العملاء.
   - `dashboard/quotations/page.tsx`: قائمة العروض.
   - `dashboard/clients/[id]/page.tsx`: استعلام أحدث الطلبات.
   - `dashboard/invoices/create/page.tsx`: استعلام العملاء واستعلام الفواتير السابقة؛ فشل الثاني قد يجعل أوردرًا مفوترًا يظهر ضمن المتاح.
10. **أخطاء قاعدة بيانات تتحول إلى 404:** قراءة العميل وتعديله وتفاصيل الفاتورة تدمج فشل الاستعلام مع «غير موجود». هذا إخفاء سبب الخطأ وليس رسالة نجاح. صفحة مشاركة العرض المعدلة أصبحت تفصل بين الخطأ وعدم وجود الصف.
11. **`scripts/seed-test-user.ts`:** خروج بكود صفر عند وجود المستخدم مسبقًا، مع رسالة صريحة تقول إنه موجود. سلوك مقصود، وليس ادعاء إنشاء مستخدم جديد.

بقية catch في واجهة AI تعرض خطأ للمستخدم، وcatch الخارجية في سكربتات seed تنهي التنفيذ بفشل. catch انتظار تشغيل الخادم في سكربت الاختبار يعيد المحاولة ثم يفشل بمهلة، ولا يقرر النجاح دون استجابة HTTP صحيحة.

## إعادة الاختبار

الاختبارات المعزولة لا تستخدم حسابات أو بيانات المصنع:

```powershell
npm.cmd run verify:batch1
node node_modules/typescript/bin/tsc --noEmit
npm.cmd run lint -- . --max-warnings=0
npm.cmd run build
```

بعد شحن نفس حساب DeepSeek، يعاد اختبار AI الحي والمشاركة فقط، دون إنشاء فواتير جديدة:

```powershell
npm.cmd run verify:live -- --ai-only
```

الاختبار الحي يحتاج Chrome وSupabase المرتبط والـ migrations الجديدة، ويستخدم المنفذين 3100 و3101. وضع `--ai-only` ينشئ ويحذف حسابًا وعرضًا مؤقتين فقط. تشغيله دون هذا الخيار يعيد أيضًا اختبارات الأوردر والبيع المباشر ويستهلك رقم فاتورة. نجاح AI الحقيقي لم يُتحقق منه بعد، وهو الجزء الوحيد المؤجل بطلب المستخدم.
