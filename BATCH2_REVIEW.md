# مراجعة Batch 2 — كشف الحساب والمرتجعات والشيكات

تم تنفيذ الدفعة وتطبيق migrations `0017` و`0018` على Supabase المرتبط. لم يبدأ أي عمل بعد هذه الدفعة. اختبار DeepSeek الحي ما زال مؤجلًا بطلب المستخدم؛ لم يُستدعَ في هذه الدفعة.

## التغييرات

- نُقل الملف التاريخي إلى [archive/DO_NOT_APPLY_schema_full.sql](archive/DO_NOT_APPLY_schema_full.sql)، مع تحذير صريح في أوله بعدم تشغيله أو تجهيز قاعدة منه.
- كشف حساب العميل يعرض كل فاتورة ودفعة ومرتجع بترتيب التاريخ، ثم وقت الإنشاء، ثم ترتيب ثابت عند التعادل، مع رصيد جارٍ وإجمالي «له / عليه». استعلام واحد يعيد JSON كاملًا من لقطة واحدة، فلا يقتطع حد PostgREST الافتراضي أول 1000 حركة. الشيك المرفوض يظل ظاهرًا بمبلغه الأصلي ودائن فعلي صفر؛ إعادة تحصيله تعيد احتسابه في نفس السطر. هذا كشف حسب حالة الشيك الحالية، وليس سجلًا مستقلًا لتاريخ تغييرات حالته.
- الفاتورة المتأخرة لها خلفية وتنبيه أحمر عندما يسبق الاستحقاق يوم القاهرة الحالي ويبقى عليها رصيد موجب. يوم الاستحقاق نفسه ليس تأخيرًا. الإصدار العادي والبيع المباشر يأخذان `credit_days` من العميل ويحفظان `due_date`؛ الفواتير القديمة ذات الاستحقاق NULL تستخدم تاريخ الإصدار + مهلة العميل. `0018` توحّد تاريخ دوال الإصدار والدفع مع يوم القاهرة، بما فيه الساعات التي يختلف فيها عن UTC.
- تعديل العميل يفحص الصف المُعاد من UPDATE؛ العميل غير الموجود يعطي خطأ دون تحويل. فشل قراءة أحدث الطلبات يعرض تنبيهًا بدل قائمة فارغة، وفشل قراءة العميل نفسه أصبح منفصلًا عن عدم وجوده.
- جدول `sales_returns` يضم الحقول المطلوبة، مع RLS مثل بقية جداول التطبيق. الحالة/السبب نص حر، والملاحظة اختيارية. تسجيل المرتجع متاح من تفاصيل الفاتورة وتظهر المرتجعات هناك وفي كشف الحساب. الدالة تتحقق من أن القيمة موجبة وألا يتجاوز مجموع المرتجعات إجمالي الفاتورة، وتقفل صف الفاتورة أثناء التسجيل.
- لا تكتب المرتجعات في `materials` ولا تنشئ `material_movements`. لا توجد إعادة خامات أو تحويل تلقائي للاسكراب. يظل إدخال خامات معاد تدويرها حركة وارد يدوية مستقلة.
- أُضيفت صفحة `/dashboard/cheques`؛ النسخة السابقة لم تحتوِ صفحة مستقلة، بل كانت تعرض الشيك داخل الفاتورة فقط. الصفحة تعرض المبلغ والعميل والفاتورة والاستحقاق والحالة، مع تصفية الحالة وإجرائي التحصيل والرفض. الإجراء يكتب `cheques.status` فقط ويتحقق من وجود الصف.

## بنية الرصيد وفحص الكتابة

أُزيل عمود `invoices.balance_due` المخزن، واستُبدل بـ [invoice_balances](supabase/migrations/0017_client_statement_sales_returns.sql)، وهو view للقراءة بصلاحيات المستخدم (`security_invoker`). الحساب في PostgreSQL numeric:

`total − SUM(payments excluding bounced cheque payments) − SUM(sales_returns)`

تُجمع المدفوعات والمرتجعات كلٌ على حدة لمنع مضاعفة المبالغ بسبب ربط عدة صفوف. الشيك المعلق والمحصّل محسوبان، والمرفوض غير محسوب. الرصيد الدائن السالب مسموح. لا توجد دالة trigger لتعديل الرصيد أو خطوة منفصلة لعكس الدفع. دوال إنشاء الفاتورة والبيع المباشر وتسجيل الدفع استُبدلت، والواجهات تقرأ الـ view؛ أنواع Insert/Update لجدول الفواتير لم تعد تحتوي `balance_due`.

**نتيجة البحث بدقة:** لا توجد كتابة مباشرة أو زيادة/نقصان تدريجي للرصيد في كود التطبيق الحالي أو الدوال/triggers الحية. أكد اختبار catalogs غياب العمود المخزن وغياب أي كاتب قديم في `pg_proc`. تبقى التعريفات التاريخية في migrations `0002`, `0007`, `0008`, `0009`, `0012`, `0013` وفي الأرشيف؛ مثلًا سطر الخصم القديم في `0009` ما زال جزءًا من سجل الترحيلات، ولكنه ليس تعريف الدالة الحية بعد `0017`. لم أعد كتابة migrations التي سبق تطبيقها لمجرد إخفاء نتائج البحث. لذلك لا أدّعي أن كلمة `balance_due` أو صيغة الخصم القديمة اختفت من التاريخ المحفوظ للريبو.

## أدلة الأرقام الفعلية

نُفذت الدورة من Chrome على التطبيق المبني، باستدعاءات Server Actions الفعلية وقاعدة Supabase الحية. كل خطوة فحصت أيضًا الـ view وكشف الحساب والرصيد الظاهر في الصفحتين:

| العملية | قبل | بعد |
|---|---:|---:|
| فاتورة اختبار إجماليها 1000 | — | عليه 1000 |
| دفعة نقدية 200 | 1000 | 800 |
| مرتجع جزئي 100 | 800 | 700 |
| دفع بشيك معلق 300 | 700 | 400 |
| رفض نفس الشيك | 400 | 700 |
| إعادة تحصيل نفس الشيك | 700 | 400 |
| تحويل بنكي 400 | 400 | 0 |
| مرتجع إضافي 50 بعد التسوية | 0 | **له 50** |
| فاتورة أخرى غير متأخرة بقيمة 25 لنفس العميل | له 50 | **إجمالي العميل: له 25** |

الدليل الحي المحفوظ: [live verification](audit/batch2/live-Batch2-813c9ecd-3ae3-4658-817d-81da6186e309.json)، ويضم UUIDs الاختبار والأرصدة الجارية لكل خطوة ونتيجة التنظيف. سجّل الاختبار أيضًا رفض تعديل عميل حُذف بعد فتح نموذج تعديله، دون تحويل نجاح مضلل. فُحص ظهور علامة التأخير للفواتير المتأخرة وعدم ظهورها للفواتير الحالية.

اختبار SQL إضافي نجح حيًا داخل معاملة انتهت بـ ROLLBACK، وبنفس الأرقام. في القاعدة المعزولة اختُبر أيضًا تكرار التحصيل دون مضاعفة الأثر، رفض دفعة/مرتجع أكبر من الحد، وانعكاس تعديل دفعة وحذف مرتجع وتغيير إجمالي فاتورة مباشرة على الرصيد. كشف من **1006 أسطر** انتهى فعليًا عند **89.95**؛ قراءة anonymous للرصيد وكشف الحساب وتسجيل المرتجع رُفضت. حقن خطأ SELECT لأحدث الطلبات أعاد تنبيه الخطأ وليس رسالة عدم وجود طلبات.

## التنظيف ومطابقة schema

الفواتير الحية الأصلية قبل الترحيل كانت 80000 و40000، والحساب الجديد طابقهما قبل إسقاط العمود القديم: [preflight.json](audit/batch2/preflight.json).

الفواتير التجريبية استعملت أرقامًا تبدأ بـ `Batch2-<UUID>`، دون استهلاك أرقام `INV`. حُذفت شيكات ودفعات ومرتجعات وفواتير وأوردرات وعميل الاختبار والحساب المؤقت. قورنت الفواتير الأصلية بعد التنظيف وكانت كما هي، وظلت أرصدة كل الخامات مطابقة وعدد حركات الخامات **2**. بقي عداد 2026 عند **3**؛ فجوة `INV-2026-0003` القديمة كما هي، ولم يحدث ترقيم جديد أو إرجاع للعداد.

أُعيد بناء القاعدة المعزولة بكل migrations من `0001` إلى `0018` ومقارنتها بـ catalogs الحية: **صفر فروق** في الحقول المقارنة. [comparison.json](audit/schema/comparison.json) ولقطتا [live.json](audit/schema/live.json) و[replayed.json](audit/schema/replayed.json) تعكس الآن الحالة بعد Batch 2. ملفات `*-before.json` الخاصة بمراجعة الانحراف السابقة لم تُعدّل. جرد التطبيق الحالي يشمل 15 جدولًا وview واحدة؛ خانة `tables` في أداة الجرد تجمع الجداول والـ views، لذلك تعرض 16.

## فحوص التسليم

- `npm.cmd run build`: نجح، 27 صفحة/مسار، منها صفحة الشيكات الجديدة.
- `node node_modules/typescript/bin/tsc --noEmit`: نجح.
- `npm.cmd run lint -- . --max-warnings=0`: نجح دون أخطاء أو تحذيرات.
- `npm.cmd run verify:batch1`: نجح، ويشمل اختبار AI المعزول فقط.
- `npm.cmd run verify:batch2`: نجح.
- `npm.cmd run verify:batch2:live`: نجح مع تنظيف fixtures وعدم تغيير العداد.
- `node scripts/audit-schema.mjs`: صفر فروق بعد `0018`.

لم يتم نشر التطبيق على استضافة خارجية. التوقف هنا لمراجعة Batch 2.
